import { navigateToggleButton } from '../utils/testEventHelper'
import { defaultInit } from '../utils/testHelperUtils'

describe('Manage notes test', () => {
  defaultInit()

  before(() => {})

  beforeEach(() => {})

  it('should add to entries by clicking toggle button', () => {
    navigateToggleButton()
  })
})
